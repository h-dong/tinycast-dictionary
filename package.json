{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "dictionary",
  "title": "Dictionary",
  "description": "Offline definitions, spelling suggestions and one-shot spelling correction from the built-in macOS dictionary and spellchecker. A stand-in for Raycast's Define Word.",
  "icon": "icon.png",
  "author": "haodong",
  "license": "MIT",
  "categories": ["Productivity"],
  "commands": [
    {
      "name": "define",
      "title": "Define Word",
      "subtitle": "Dictionary",
      "description": "Look up a word in the macOS dictionary, with spelling suggestions as you type.",
      "mode": "view",
      "arguments": [
        { "name": "word", "placeholder": "Word", "type": "text", "required": false }
      ]
    },
    {
      "name": "correct",
      "title": "Correct Spelling",
      "subtitle": "Dictionary",
      "description": "Fix misspelled words in the selected text and replace it in place. Falls back to the clipboard when nothing is selected.",
      "mode": "no-view"
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.100.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/react": "^19.0.0",
    "typescript": "^5.6.0"
  },
  "scripts": {
    "build": "ray build -e dist -o build",
    "dev": "ray develop"
  }
}
